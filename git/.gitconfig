[difftool]
    trustExitCode = true

[pager]
    diff =

[alias]
    a = add
    b = branch
    bd = branch -d
    bD = branch -D
    bm = branch -m
    bsc = branch --show-current
    c = commit
    ca = commit --amend
    co = checkout
    cob = checkout -b
    com = checkout master
    comn = checkout main
    d = diff
    dc = diff --cached
    dh = diff HEAD~
    dm = diff master HEAD
    dmn = diff main HEAD
    dmc = "!git diff --name-only HEAD master | cat"
    dmerged = "!git branch --merged | grep -v '\\*' | xargs -n 1 git branch -d"
    dmnc = "!git diff --name-only HEAD main | cat"
    l = log
    merged = branch --merged
    mm = merge master
    mmn = merge main
    pl = pull
    ps = push
    poh = push -u origin HEAD
    pom = pull origin master
    pomn = pull origin main
    pfoh = push -fu origin HEAD
    rb = rebase
    rba = rebase --abort
    rbc = rebase --continue
    rbi = rebase -i
    rbim = rebase -i master
    rbm = rebase master
    rbs = rebase --skip
    s = status
    st = status
    unstage = reset --soft HEAD^
  xx = "!git branch --merged | grep -v '\\*' | xargs -n 1 git branch -d"

[core]
    editor = vim
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/

[include]
    path = .gitconfig.local
[gpg]
	format = ssh
[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign
[commit]
	gpgsign = true
[user]
	signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIARVAiU8oqD4aI60xhIf7okHUGc6e9fBUccL4uYpVc0T
	name = Richard Wong
	email = rwong@wealthsimple.com
